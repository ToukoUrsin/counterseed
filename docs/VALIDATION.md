# Validation

Local validation, September 22, 2026 UTC (September 21 PDT):

- `npm test`: 12 suites passed, no failures.
- All 33,867 labeled graphs through six vertices compared against independent core-property oracles.
- All 24 properties compared on every graph through five vertices and every 127th six-vertex mask, deterministically.
- Exact expected minimal witnesses for triangle-free/bipartite, no-isolates/connected and minimum-degree/bridgeless.
- Full Euler implication census: 33,867 checked, zero violations.
- Cancellation at 256 visited graphs produces an interrupted result rather than an assertion of completeness.
- Tampering with counts, witness encoding or property values is rejected by the independent verifier.

The examples directory contains actual exports generated by the same public search engine and checked with the independent CLI. Counts and fingerprints can be regenerated.

No GitHub Actions passing status is claimed: the account's execution was reported blocked by billing. That external limitation does not substitute for, or negate, the completed local tests. Hosting availability is separate from test execution.

The mathematics examples are known graph-theory facts and counterexamples. There is no claim of a newly discovered theorem or of measured educational impact.
